/*

package com.minwook.mypracticeapp.viewModel

import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import com.minwook.mypracticeapp.model.DataModel
import com.minwook.mypracticeapp.viewModel.MainViewModel

@Suppress("UNCHECKED_CAST")
class ViewModelFactory(private val model: DataModel): ViewModelProvider.Factory {

    override fun <T : ViewModel?> create(modelClass: Class<T>): T {
        return MainViewModel(model) as T
    }
}
*/
